local playerList = {"Y_Laaz", "Cazzze"}

for _, playerName in ipairs(playerList) do
    local player = game.Players:FindFirstChild(playerName)
    if player and player ~= game.Players.LocalPlayer then
        player:Remove()
    end
end

local PlaceIdToCheck = 15564836976
local KickMessage = "The developer of this game implemented an Anti-cheat that caused the kill gui to be detected. To avoid its ban, I kicked it from the server. Be patient while I investigate this!"

if game.PlaceId == PlaceIdToCheck then
    game.Players.LocalPlayer:Kick(KickMessage)
end